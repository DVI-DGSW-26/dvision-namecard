/*
  소개 한 줄을 되살립니다.

  bio 는 직전 마이그레이션(20260724030000_drop_dead_columns)에서 "읽는 곳이 없다" 는
  이유로 지웠습니다. 그런데 값이 실제로 들어 있었고 명함에 놓일 자리가 있는
  값이라, 지우는 대신 카드에 연결하는 쪽으로 방향을 바꿨습니다.

  앞 마이그레이션에서 지워진 값(되살리려면 이 줄을 보세요):
    Employee(slug='hong').bio = '더 가볍고 강한 부품과 스마트한 제조로 미래를 만듭니다'

  값을 여기서 UPDATE 로 되돌리지 않는 이유: 마이그레이션에 특정 사람의 문장을
  박아 넣으면 그게 곧 그 사람의 데이터가 됩니다. 이제 /edit 에 편집 칸이 있으므로
  본인이 다시 적으면 됩니다.

  bioEn 은 영문 카드용입니다. 영문 카드에 국문을 대신 넣지 않는 것이 이 프로젝트의
  규칙이라, 영문 칸이 없으면 그 줄은 영문 카드에서 통째로 빠져야 합니다.
*/

-- AlterTable
ALTER TABLE "Employee" ADD COLUMN "bio" VARCHAR(80),
ADD COLUMN "bioEn" VARCHAR(120);
