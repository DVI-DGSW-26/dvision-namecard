/**
 * 직원 선택 + 정보 수정. (스텁, 쿠키 필요 — middleware 가 보호)
 *
 * 구현 시: 공용 비밀번호 방식이라 "내가 누구인지"를 서버가 모릅니다.
 * 직원 목록에서 본인을 고르게 한 뒤 PATCH /api/employees/[id] 로 저장하세요.
 */
export default function EditPage() {
  return <main>직원 정보 수정 (스텁)</main>;
}
