/**
 * 이메일 서명 복사 + 설치 안내. (스텁, 쿠키 필요)
 *
 * 구현 시: buildSignatureHtml() 결과를 미리보기로 렌더하고,
 * 클립보드에는 text/html 로 복사해야 메일 클라이언트에 서식이 유지됩니다.
 * (평문으로 복사하면 태그가 그대로 붙습니다.)
 */
export default function SignaturePage() {
  return <main>이메일 서명 (스텁)</main>;
}
