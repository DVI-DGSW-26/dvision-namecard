/**
 * 공용 비밀번호 입력 화면. (스텁)
 *
 * 구현 시: POST /api/gate 로 비밀번호 전송 → 성공하면 ?next 쿼리의 경로로 이동.
 * ?next 는 사용자가 조작할 수 있으므로 반드시 내부 경로(`/` 로 시작, `//` 아님)인지
 * 확인하고 쓸 것. 아니면 오픈 리다이렉트가 됩니다.
 */
export default function GatePage() {
  return <main>비밀번호 입력 (스텁)</main>;
}
